CREATE TABLE employee (
  emp_id NUMBER PRIMARY KEY,
  name VARCHAR2(20),
  salary NUMBER
);

INSERT INTO employee VALUES (1, 'Aadi', 20000);

INSERT INTO employee VALUES (2, 'Rohit', 30000);

SELECT * FROM employee;

CREATE OR REPLACE TRIGGER salary_trigger
BEFORE INSERT ON employee
FOR EACH ROW
BEGIN
  :NEW.salary := :NEW.salary + 1000;
END;
/

INSERT INTO employee VALUES (3, 'Rahul', 15000);

SELECT * FROM employee;